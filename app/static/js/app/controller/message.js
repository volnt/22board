app.controller("MessageCtrl", function($scope, $http, $routeParams, $location, Page) {

  $scope.Page = Page;

  $scope.main = function() {
    /*
    * Entry point of the controller.
    */
  }();
});
