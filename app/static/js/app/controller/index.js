blogApp.controller("IndexCtrl", function($scope, $http, $routeParams, Page) {

  $scope.loading = true;
  $scope.Page = Page;

  $scope.main = function() {
    /*
    * Entry point of the controller.
    */
  }();
});
